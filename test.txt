{
  "repo_url": "https://github.com/manimaranRadhakrishnan/demo1.git",
  "branch_name": "test12",
  "base_branch": "main",
  "repo_provider": "github"
}



# 3. Create PR
                # pull_request=self.create_pull_request(
                #     token=self.token,
                #     owner=self.username,
                #     repo="temenos",
                #     head_branch=new_branch,
                #     base_branch=base_branch,
                #     title="Feature 1 PR",
                #     body="Please review my changes"
                # )

                # print(pull_request,"5555555555555555555555")


 def create_pull_request(
        self,
        token: str,
        owner: str,
        repo: str,
        head_branch: str,
        base_branch: str,
        title: str,
        body: str = ""
    ):
        url = f"https://api.github.com/repos/{owner}/{repo}/pulls"

        print(url,"1111111111111111111111111111")
    
        headers = {
            "Authorization": f"token {token}",
            "Accept": "application/vnd.github.v3+json"
        }
    
        payload = {
            "title": title,
            "head": head_branch,
            "base": base_branch,
            "body": body
        }
    
        response = requests.post(url, json=payload, headers=headers)


        print(response,"fffffffffffffffffffffffff")
    
        if response.status_code == 201:ssss
            print(response,"jjjjjjjjjjjjjjjjjjjjjjjjjjjjjjj")
            return {
                "message": "Pull Request created",
                "pr_url": response.json()["html_url"]
            }
        else:
            raise Exception(response.json())
    
